# AIビールソムリエ Trainer

このレポジトリは、ユーザーの好みやビールの特徴を学習するための **trainer.py** を管理するプロジェクトです。
Streamlit などの UI と連携して、ビール推薦やスタイル比較学習を行います。

---

## 📂 ファイル構成

```
beer_sommelier/
├─ trainer.py           # 学習処理・フィードバック管理
├─ beer_data.xlsx       # ビールデータベース（必須列: beer_id, name, style）
├─ user_feedback.json   # ユーザーの選択・説明文データ（trainer.py が自動作成）
├─ requirements.txt     # 必須Pythonライブラリ
└─ README.md            # このファイル
```

---

## ⚙️ セットアップ

Python 3.10 以上を推奨

```bash
# 仮想環境作成（推奨）
python -m venv venv
source venv/bin/activate  # macOS/Linux
venv\Scripts\activate     # Windows

# 必須ライブラリインストール
pip install -r requirements.txt
```

---

## 📝 使い方

```python
from trainer import BeerTrainer

trainer = BeerTrainer()

# フィードバック追加
trainer.add_feedback("beer_001", 1, "香りが華やかでジューシー", style="Hazy IPA")

# トップビール取得
top_beers = trainer.get_top_beers()
print(top_beers)

# スタイル別説明文取得
examples = trainer.get_style_examples("Hazy IPA")
print(examples)
```

---

## 📊 データについて

* `beer_data.xlsx`：ビールの情報を管理（beer_id, name, style など）
* `user_feedback.json`：ユーザーの選択と説明を蓄積
* trainer.py がこれらを読み込み・更新して、あなた専用の学習モデルとして活用可能

---

## ⚡ 参考

* Excel データの beer_id はユニークにしてください
* 大量データや高度な学習が必要な場合は SQLite やベクトルDBへの移行も可能
